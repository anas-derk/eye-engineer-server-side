const USER_TYPES = ["user", "admin"];
const USER_PROVIDERS = ["same-site", "google"];
const DEFAULT_USER_PROVIDER = USER_PROVIDERS[0];

module.exports = {
    USER_TYPES,
    USER_PROVIDERS,
    DEFAULT_USER_PROVIDER
}